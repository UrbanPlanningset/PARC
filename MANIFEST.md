# Release Manifest

Included:

- current PARC implementation under `src/microupdate/`;
- current physics, RL, agent, baseline, ablation, and audit scripts;
- minimal configuration files;
- installation, data-layout, and credential templates.

Excluded:

- `.git` history and editor settings;
- `.env` and all credentials;
- local/remote absolute paths;
- raw and processed data;
- SOLWEIG caches and outputs;
- trained model weights and checkpoints;
- experiment results, figures, paper files, and internal notes;
- virtual environments, compiled caches, logs, and archived prototypes.
